# Assets

Place `demo.gif` here showing the full ShopifyQL skill flow:

1. User asks Claude a business question
2. Claude writes the ShopifyQL query
3. User says "run it"
4. Executor agent runs the query
5. Results appear as a table in chat

Recording tips:
- Use terminal recording (Quicktime on macOS)
- Convert to GIF with gifski or ffmpeg
- Keep under 5MB for GitHub inline rendering
- 20-30 second loop
